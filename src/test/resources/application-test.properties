# ─────────────────────────────────────────────────────────────
# Activated during tests — provides dummy values so Spring
# context loads without real AWS or DB connections
# ─────────────────────────────────────────────────────────────
 
# ===== DynamoDB (dummy value — no real DB in tests) =====
aws.dynamodb.table-name=tasks-test 
# dummy — no real DB needed in tests
 
# ===== Disable H2 console in tests =====
spring.h2.console.enabled=false
 
# ===== Suppress noisy logs during tests =====
logging.level.root=WARN
logging.level.com.taskmanager=DEBUG
